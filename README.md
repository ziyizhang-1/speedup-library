# speedup-library

## Quick Guide

### Run the unittest directly
```
cargo run
```

### Build the native Python module and install
```
python -m venv .env
source .env/bin/activate
pip install maturin
maturin develop
```